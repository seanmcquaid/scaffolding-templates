.PHONY: setup dev build test test-watch test-coverage lint lint-fix

setup:
	chmod +x setup.sh && ./setup.sh

dev:
	pnpm dev

build:
	pnpm build

test:
	pnpm test

test-watch:
	pnpm test:watch

test-coverage:
	pnpm test:coverage

lint:
	pnpm lint

lint-fix:
	pnpm lint:fix
